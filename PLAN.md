# 몽진 (蒙塵) — 게임 디자인 & 개발 계획

> 작성일: 2026-06-12 · 갱신: 2026-06-12
> 상태: **v0.3 규칙 확정·배포 완료** — 플레이테스트에서 발견된 "왕 단독 돌진 필승" 결함을
> 왕 잡기 규칙으로 수정 후 규칙 고정(토글 UI 제거), GitHub Pages 공개.
>
> - **플레이**: https://kamilokwon.github.io/mongjin/
> - **저장소**: https://github.com/KAMILOKWON/mongjin (main 푸시 시 자동 배포)

---

## 1. 게임 콘셉트

- **이름**: 몽진(蒙塵) — "임금이 난리를 피해 도성을 떠남". 왕을 호위하며 피난길(목적지)로 옮기는 테마와 일치
- **장르**: 2인 추상 전략 보드게임 (장기/오목/바둑 계열)
- **대결 구도**: 흑 vs 백, 1:1
- **핵심 목표**: 자신의 **왕(王) 말**을 정해진 목적지까지 상대보다 먼저 옮기면 승리
- **핵심 메커니즘**: 매 턴 **"새 말 두기"** 또는 **"말 옮기기"** 중 하나를 선택 (Hive, 따라서 착수와 기동의 긴장감이 핵심)
- **말 개수 제한**: 호위 말은 정해진 개수까지만 둘 수 있다 → 자원 관리 요소

참고할 유사 게임 (규칙 설계 시 벤치마크):
- **타플(Hnefatafl)**: 왕을 목적지로 탈출시키는 비대칭 목표 구조
- **하이브(Hive)**: "두기 or 옮기기" 선택 구조, 말 개수 제한
- **브레이크스루(Breakthrough)**: 상대 진영 도달 경쟁

---

## 2. 규칙 설계 (v0.1 제안)

아래는 첫 프로토타입용 제안 규칙이다. `⚖️` 표시는 플레이테스트로 결정할 항목.

### 2.1 보드
- **9×9 격자** (칸 위에 말을 놓는 방식, 장기식) `⚖️ 7×7 / 11×11 비교 테스트`
- 흑은 아래쪽, 백은 위쪽 진영에서 시작
- **목적지**: 상대 진영 맨 끝 줄(9번째 행)의 **중앙 3칸** `⚖️ 끝줄 전체 vs 중앙 3칸 vs 중앙 1칸`

### 2.2 말 구성 (플레이어당)
| 말 | 개수 | 시작 위치 |
|---|---|---|
| 왕(王) | 1 | 자기 진영 맨 끝 줄 중앙 (고정 배치) |
| 호위(衛) | 최대 **8개** `⚖️ 6~10 테스트` | 시작 시 0개, 게임 중에 착수 |

### 2.3 턴 진행
선공은 흑. 자신의 턴에 아래 둘 중 **하나만** 수행한다.

1. **두기(착수)**: 손에 남은 호위 말 1개를 빈 칸에 놓는다.
   - 착수 가능 범위: **자기 말(왕 포함)과 상하좌우로 인접한 빈 칸** `⚖️ 자기 진영 절반 안 어디든 vs 인접 칸` 
   - → 인접 착수 규칙이면 "왕의 행렬이 전진하며 호위를 전개하는" 그림이 나옴
2. **옮기기(이동)**: 자기 말 1개를 이동시킨다.
   - **왕**: 상하좌우 + 대각선 1칸 (체스 킹과 동일)
   - **호위**: 상하좌우 1칸 `⚖️ 직선 슬라이드(차/룩식)도 후보 — 너무 강하면 1칸 유지`

### 2.3.1 호위 목적지 금지 (2026-06-12 추가, 기본 켬)
- 호위는 **양쪽 목적지 칸에 착수할 수 없고, 이동으로 멈출 수도 없다** (`noGuardOnGoal`)
- 목적: 호위 2~3개로 상대 목적지를 영구 봉쇄하는 거북이 전략 차단
- 부수 효과 1: 자기 진영 끝줄 중앙(=상대 목적지)에도 못 두므로, 첫 수 착수 칸이 왕 앞 1칸뿐
- 부수 효과 2: 무작위 셀프플레이 평균 길이 585수 → 444수로 단축 (봉쇄성 정체 감소 확인)
- ⚠️ 상호작용 주의: 이 규칙이 켜지면 목적지 옆 칸에 호위를 못 두므로, **집에 눌러앉은 왕을 포위로 응징하기 어려워진다** (포위 규칙과 길항). 플레이테스트에서 켬/끔 A/B 비교 필요

### 2.4 잡기(전투) — v0.3에서 대폭 개정
- **호위**는 상대 호위가 있는 칸으로 이동해 **잡을 수 있다** (장기식 대체 잡기)
- 잡힌 호위는 게임에서 제거된다 (손으로 돌아가지 않음) `⚖️ 샤기식 "잡은 말 재사용"은 복잡도 상승 — v2 후보`
- **[v0.3 개정] 호위는 상대 왕도 잡을 수 있다 — 왕이 잡히면 즉시 패배** (`kingCapture`, 기본 켬)
  - 첫 플레이테스트에서 치명적 결함 발견: 왕이 무적이면 "왕 단독 돌진"이 필승 전략이 되어 호위가 잉여 자원이 됨 (포위 4방은 움직이는 왕에게 사실상 불가능)
  - 왕 잡기 도입 후: 호위 1개만 진격로에 붙어도 왕이 위협받음 → **호위 동반 진격이 강제됨** (테마 그대로)
  - 왕 잡는 수는 목적지 금지(2.3.1)의 예외 — 목적지 칸에 앉은 왕도 잡을 수 있다
  - 무작위 셀프플레이 종료 사유 변화: 목적지 95% → **잡기 81% / 목적지 18%**, 평균 길이 444수 → 216수
  - AI 검증: 왕 단독 돌진 전략을 AI(백)가 추격·포획해 승리 확인
- 왕은 여전히 잡기를 하지 못한다 (빈 칸으로만 이동)
- 왕 포위(상하좌우 4방 봉쇄) 패배 규칙은 유지하되, 왕 잡기 도입으로 발동 빈도는 낮아짐 `⚖️ 잡기 정착 시 제거 검토`

### 2.5 승리 / 무승부
- **승리**: 자신의 왕이 목적지 칸에 **자기 턴 종료 시점**에 도달
- **패배(보조)**: 자신의 왕이 어떤 합법 수로도 움직일 수 없고, 둘 호위도 없으면 패배 `⚖️`
- **무승부 방지**: 동형 국면 3회 반복 시 반복을 만든 쪽 패배 (또는 무승부) `⚖️`

### 2.6 밸런스 우려 사항 (테스트 포인트)
- **선공 우위**: 양쪽 다 왕만 직진하면 두 왕 모두 8수 — 흑이 정확히 1템포 먼저 도착해 이긴다. 백은 반드시 수비에 수를 써야 하므로 실질 밸런스는 플레이테스트로 확인. 보정 후보: 백 호위 +1, 파이 규칙(첫 수 후 진영 선택)
- **거북이 전략**: 왕 앞을 호위로 둘러싸고 전진만 하는 전략이 무적이면 안 됨 → 잡기 규칙/포위 패배/목적지 금지(2.3.1)로 견제
- **가장자리 포위**: 변의 왕은 3개, 구석의 왕은 2개 말로 포위된다 — 측면 우회의 리스크가 의도된 긴장인지 과한 페널티인지 관찰
- **반복 패배의 책임**: 봉쇄를 찔러보는 공격자가 오히려 반복패를 당할 수 있음 — 무승부 처리로 바꿀지 관찰
- **말 소진**: 호위 8개를 다 잃으면 사실상 패배인지, 역전 가능한지 확인

### 2.7 검토 후 보류한 변형: 꼭짓점 → 꼭짓점 레이스 (2026-06-12)
왕이 자기 구석에서 대각 반대편 구석으로 가는 변형을 검토했으나 **현행(중앙 출발 → 중앙 3칸 도착)이 게임성 우위**로 판단:
- 9×9에서 구석→구석 최단 경로는 **대각선 단 1줄** (매 수 대각 이동 강제) — 수비자가 막을 줄을 정확히 알아 호위 1개의 견제 효율이 과도함. 중앙판은 최단 경로가 수십 갈래라 공격자에게 경로 선택권이 있다
- 구석 목적지는 진입구가 3칸뿐 — 호위 2~3개로 초크 가능, 거북이 메타 유발. 게다가 포위 규칙상 구석 근처 왕은 2개 말로 포위돼 종반이 도박이 된다
- 두 왕이 같은 대각선을 역주행하며 중앙에서 정면 충돌 — 잡히지 않는 왕끼리 길막 교착 → 반복 규칙 복권전이 될 위험
- 행동 반경이 대각선 띠에 압축돼 보드의 두 삼각형 영역이 죽은 공간이 됨
- (살릴 만한 절충안) 구석의 맛을 원하면 **"상대 진영 양쪽 구석 2곳 중 아무 곳 도달"** 변형이 후보 — 경로가 2갈래로 갈려 수비자가 병력을 쪼개야 하므로 단일 구석의 문제가 완화됨. Phase 2 이후 토글로 실험 가치 있음

---

## 3. 기술 설계

### 3.1 스택
- **TypeScript + Vite** 웹 앱 (설치 없이 브라우저에서 바로 플레이 → 플레이테스트 반복에 최적)
- 렌더링: **SVG 또는 DOM 그리드** (9×9 수준이면 Canvas 불필요, 클릭 처리 간단)
- 테스트: **Vitest** (규칙 엔진 단위 테스트)
- 상태 관리: 외부 라이브러리 없이 순수 함수형 게임 상태로 시작

### 3.2 아키텍처 — 규칙과 화면의 분리
```
src/
├── core/              # 순수 게임 로직 (DOM 의존성 0, 전부 단위 테스트 대상)
│   ├── types.ts       # GameState, Move, Piece, Player 타입
│   ├── rules.ts       # 합법 수 생성: legalMoves(state) → Move[]
│   ├── apply.ts       # 수 적용: applyMove(state, move) → GameState (불변)
│   └── result.ts      # 승패 판정: getResult(state) → 'BLACK'|'WHITE'|'DRAW'|null
├── ai/                # (3단계) 미니맥스 + 평가 함수
├── ui/                # 보드 렌더링, 입력 처리, 턴 표시
└── main.ts
```

핵심 원칙:
- `core/`는 **불변(immutable) 상태 + 순수 함수**로만 작성 → AI 탐색, 되돌리기(undo), 리플레이가 공짜로 따라옴
- 모든 규칙 변경(`⚖️` 항목)은 `rules.ts`의 **설정 객체(RuleConfig)** 로 빼서 플레이테스트 중 토글 가능하게 한다
  ```ts
  interface RuleConfig {
    boardSize: 7 | 9 | 11;
    guardCount: number;        // 6~10
    goalCells: 'full-row' | 'center-3' | 'center-1';
    placement: 'adjacent' | 'own-half';
    guardMove: 'step' | 'slide';
    kingSurroundLoss: boolean;
  }
  ```

### 3.3 게임 상태 모델 (초안)
```ts
type Player = 'BLACK' | 'WHITE';
type PieceType = 'KING' | 'GUARD';

interface GameState {
  board: (Piece | null)[][];      // 9x9
  turn: Player;
  guardsInHand: { BLACK: number; WHITE: number };
  history: Move[];                // 반복 수 검사 + 리플레이용
}

type Move =
  | { kind: 'PLACE'; to: Coord }
  | { kind: 'MOVE'; from: Coord; to: Coord };
```

---

## 4. 개발 로드맵

### Phase 1 — 규칙 엔진 + 로컬 대전 (MVP) ✦ 완료 (2026-06-12)
- [x] 프로젝트 셋업 (Vite + TS + Vitest)
- [x] `core/` 구현: 상태, 합법 수 생성, 수 적용, 승패 판정
- [x] 규칙 단위 테스트 13개 (착수 범위, 이동, 잡기, 승리 조건, 포위, 반복 수)
- [x] 보드 UI: 클릭으로 두기/옮기기, 합법 수 하이라이트, 턴/남은 말 표시
- [x] **같은 화면에서 흑백 번갈아 두는 핫시트(hot-seat) 대전**
- [x] undo / 리셋 / 규칙 설정(RuleConfig) 토글 패널

실행: `npm install && npm run dev` → http://localhost:5173 · 테스트: `npm test`

**→ Phase 1 완료 시점에 사람끼리 플레이테스트를 돌려 `⚖️` 항목들을 확정한다. 이게 전체 일정의 가장 중요한 관문.**

### Phase 2 — 게임 완성도
- [ ] 확정된 규칙으로 RuleConfig 기본값 고정
- [ ] 애니메이션, 마지막 수 표시, 잡힌 말 표시
- [ ] 기보 저장/리플레이 (history 직렬화)
- [ ] 모바일 터치 대응

### Phase 3 — AI 상대 (1인 플레이) ✦ v2 완료 (2026-06-12)
- [x] 네가맥스 + 알파베타 + 수 정렬 + **반복 심화(시간 700ms 한도, 최대 깊이 9)**
- [x] UI "상대: 컴퓨터(백)" 모드 (기본값) — 사람 수 이후 자동 응수, 무르기는 한 쌍(2수) 단위
- [x] **평가 함수 v2** (v1 "너무 멍청함" 피드백으로 재설계):
  - 왕 골 거리: 체비셰프 직선거리 → **위험 지대 인지 BFS** (말이 막은 칸 + 상대 호위
    사정거리 칸은 통과 불가; 목적지 칸은 즉시 승리라 예외) → 호위 길목 수비가 평가에 잡힘
  - **레이스 모드 전환**: 도착 플라이 비교(2·myD−1 vs 2·oppD)로 우위/열세를 가르고,
    열세면 내 전진은 무가치 — 상대 지연(+25/칸)과 침입 왕 길목(골 쪽 2칸 앞) 선점에 집중.
    선형 평가(−14)로는 "이미 진 레이스를 계속 달리는" 수가 수비보다 높게 평가되는 결함 수정
  - 호위 재질(+3), 왕 위협(±60/5000), 호위 행렬 보너스(+5/근접 호위)
- [x] 강함 회귀 테스트: 무작위 변주 "왕 단독 돌진" 봇 상대 전승, 무작위 봇 상대 전승
- [ ] 난이도 단계 (시간/깊이 토글), 수비 진형 고도화

### Phase 4 — 온라인 대전 (선택)
- [ ] WebSocket 서버 (방 코드로 친구 초대 방식이 가장 단순)
- [ ] 서버는 `core/` 로직을 그대로 재사용해 수 검증 (치팅 방지)

---

## 5. 결정 사항 / 오픈 퀘스천

1. ~~게임 이름~~ → **몽진(蒙塵)** 확정
2. ~~보드 크기 기본값~~ → 9×9 채택 (설정 패널에서 7/11 테스트 가능)
3. ~~왕 포위 패배 규칙~~ → **기본 켬**으로 채택 (설정에서 끌 수 있음)
4. (보류) 플랫폼 — 웹 우선, 추후 모바일 앱(예: Capacitor 래핑) 검토

---

## 6. 다음 액션

1. 핫시트 프로토타입으로 플레이테스트 → 남은 `⚖️` 항목(호위 개수, 목적지 폭, 호위 이동 방식, 착수 범위) 확정
2. 선공(흑) 우위 여부 관찰 → 필요 시 백 보상 규칙 설계
3. 규칙 확정 후 Phase 2(완성도) 착수, 이 문서 v0.3으로 업데이트
