# Mongjin

Read and follow `AGENTS.md` in this directory before doing any work. It defines the current development target (`apps/mobile`, React Native + Expo — no SwiftUI app), the repository layout, and the workspace rules.
